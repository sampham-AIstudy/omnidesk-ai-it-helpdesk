# ============================================================
# Help Desk AI Agent — Dependencies
# ============================================================

# --- Core Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
python-multipart>=0.0.9

# --- AI / LangChain / LangGraph ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-mistralai>=0.2.0
langgraph>=0.2.0
langsmith>=0.1.0

# --- Vector Store (RAG) ---
chromadb>=0.5.0
sentence-transformers>=3.0.0

# --- Database ---
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
alembic>=1.14.0

# --- Auth / Security ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# --- HTTP / Utilities ---
httpx>=0.28.0
anyio>=4.0.0

# --- Local MCP servers ---
# The bundled servers import ``mcp.server.fastmcp``, which is the MCP 1.x API.
mcp>=1.6.0,<2.0.0

# --- Cache (standard Redis + Upstash REST fallback) ---
redis>=5.0.0
upstash-redis>=1.1.0

# --- Observability ---
# LangSmith remains optional for AI evaluation/debugging. OpenTelemetry provides
# vendor-neutral distributed tracing and operational metrics.
opentelemetry-api>=1.30.0
opentelemetry-sdk>=1.30.0
opentelemetry-exporter-otlp-proto-grpc>=1.30.0
opentelemetry-instrumentation-fastapi>=0.51b0
opentelemetry-instrumentation-httpx>=0.51b0
opentelemetry-instrumentation-sqlalchemy>=0.51b0

# The in-repository evaluation runner works without RAGAS.  Do not add RAGAS
# here: its current dependency chain is incompatible with this project's
# LangChain 1.x stack and has no Windows Python 3.14 scikit-network wheel.

# --- Security & Guardrails ---
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
google-genai>=0.1.0
requests>=2.31.0

# --- Dev / Test ---
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
