{
  "schema_version": 1,
  "purpose": "Deterministic Step 7 orchestration evaluation; it does not alter locked retrieval datasets.",
  "cases": [
    {"id": "ADAPT-01", "kind": "strong", "query": "BitLocker recovery key", "expected_source": "web-bitlocker-recovery-001", "expect_retry": false, "max_evidence": 1, "required_facts": ["A BitLocker recovery key is a 48-digit value used to regain access to an encrypted drive."], "optional_facts": ["Record the first eight digits of the recovery-key ID before looking for the key."], "forbidden_unrelated_facts": ["TCP service-listening diagnostics"]},
    {"id": "ADAPT-02", "kind": "strong", "query": "HTTP 403 Forbidden", "expected_source": "p0-10-http-403-forbidden-c001", "expect_retry": false, "max_evidence": 1, "required_facts": ["HTTP 403 means the server understood the request but refuses authorization.", "HTTP 403 is not a TCP port timeout or closed-port diagnosis."], "optional_facts": ["Check identity, authorization policy, web-server or reverse-proxy rules, and audit logs."], "forbidden_unrelated_facts": ["TCP port 403 is an HTTP status"]},
    {"id": "ADAPT-03", "kind": "strong", "query": "connection refused", "expected_source": "p0-04-service-listening-c001", "expect_retry": false, "max_evidence": 1, "required_facts": ["Confirm that the intended process is running and listening on the intended address and TCP port.", "A firewall rule does not start a non-listening service."], "optional_facts": ["Record listener address, port, process identity, and firewall profile."], "forbidden_unrelated_facts": ["HTTP authorization diagnosis"]},
    {"id": "ADAPT-04", "kind": "strong_technical", "query": "ping duoc nhung port khong vao", "expected_source": "p0-02-l3-vs-l4-c001", "expect_retry": false, "max_evidence": 1, "required_facts": ["Successful ping proves only ICMP/L3 reachability, not that a TCP service port is reachable."], "optional_facts": ["Check service listening and firewall policy for TCP connectivity."], "forbidden_unrelated_facts": ["VPN authentication failure"]},
    {"id": "ADAPT-05", "kind": "unrecoverable_no_kb", "query": "proprietary-widget-zz99 failure", "expect_retry": true, "max_passes": 2, "required_facts": [], "optional_facts": [], "forbidden_unrelated_facts": ["Generic historical knowledge-gap content"]},
    {"id": "ADAPT-06", "kind": "fresh", "query": "Microsoft latest current Windows VPN advisory", "expect_web_policy": true},
    {"id": "ADAPT-07", "kind": "direct", "query": "xin chao", "expect_retrieval": false},
    {"id": "ADAPT-08", "kind": "social", "query": "hom nay an gi", "expect_retrieval": false}
  ]
}
